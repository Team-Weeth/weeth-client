# 레퍼런스

외부 자료, 학습 노트, 라이브러리 사용법 메모. 우리 결정의 *근거*가 된 자료는 ADR에서 여기로 링크한다.

## 분류 가이드 (선택)
- `react/` — React 19, 컴파일러
- `nextjs/` — App Router, RSC, Server Actions
- `tiptap/` — 에디터 사용법
- `tanstack-query/` — 캐시 전략

폴더는 필요해질 때 만든다. 처음부터 빈 폴더로 두지 않는다.
